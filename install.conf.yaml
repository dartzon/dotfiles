- defaults:
    link:
      create: true
      force: true

- clean: ['~']

- link:
    ~/.bashrc: profile
    ~/.config/git: gitconf/
    ~/.gitconfig: gitconf/gitconfig
    ~/.profile: profile

- shell:
  - [git submodule update --init --recursive, Installing submodules]
